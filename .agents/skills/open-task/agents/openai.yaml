interface:
  display_name: "Open Task"
  short_description: "Start or resume a task record"
  default_prompt: "Open a task for the current project: find or create the right task record in .work/tasks, preview the intended change, and update it after confirmation."
policy:
  allow_implicit_invocation: false
