interface:
  display_name: "Close Task"
  short_description: "Update and close out a task record"
  default_prompt: "Close out a task for the current project: collect the session outcome, optionally do a brief review against relevant human-learning items, preview the updates, then write them after confirmation."
policy:
  allow_implicit_invocation: false
